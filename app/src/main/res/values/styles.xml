<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="WeiboContentStyle">
        <item name="android:textColor">@color/colorTextPrimary</item>
    </style>

    <style name="WeiboTimestampStyle">
        <item name="android:textColor">@color/colorTextPrimaryLighter</item>
    </style>

    <style name="RetweetWeiboStyle">
        <item name="android:background">@drawable/repost_background</item>
    </style>

    <style name="OccupationProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="android:background">@color/colorTransparentDark</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
